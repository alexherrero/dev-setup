#!/usr/bin/env pwsh
# install-gui-apps.ps1 — Windows stub (PLAN.md task 9).
#
# Real implementation deferred. Target: winget IDs for Antigravity,
# Gemini Desktop, and Claude Desktop if they're published; otherwise
# the browser-assisted shape from install-gui-apps.sh (poll
# %ProgramFiles% / %LOCALAPPDATA% for the expected installed path and
# strip SmartScreen's "Mark-of-the-Web" if necessary). See docs/windows.md.

$ErrorActionPreference = 'Stop'

Write-Host '==> gui-apps (Windows)'
Write-Host '    TODO: implement on Windows reference VM'
exit 0
