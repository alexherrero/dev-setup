#!/usr/bin/env pwsh
# install-brew.ps1 — Windows stub (PLAN.md task 9).
#
# Real implementation deferred until a Windows reference VM is wired up.
# Target: winget- or Chocolatey-based installs of the Windows equivalents
# of the macOS formulae (node, gh, jq, ripgrep, shellcheck, shfmt).
# See docs/windows.md for the full deferral rationale.

$ErrorActionPreference = 'Stop'

Write-Host '==> brew (Windows)'
Write-Host '    TODO: implement on Windows reference VM'
exit 0
