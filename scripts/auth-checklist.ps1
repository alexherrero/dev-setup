#!/usr/bin/env pwsh
# auth-checklist.ps1 — Windows stub (PLAN.md task 9).
#
# The checklist content itself is platform-neutral; this will be a near-
# verbatim port of scripts/auth-checklist.sh once the preceding Windows
# stages exist to provision the tools it references. Held as a stub so
# the stage list remains coherent across platforms. See docs/windows.md.

$ErrorActionPreference = 'Stop'

Write-Host '==> auth-checklist (Windows)'
Write-Host '    TODO: implement on Windows reference VM'
exit 0
