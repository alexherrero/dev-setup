#!/usr/bin/env pwsh
# install-clis.ps1 — Windows stub (PLAN.md task 9).
#
# Real implementation deferred. Target: Anthropic's Windows installer
# for Claude Code (if it exists — otherwise npm global), and
# `npm install -g @google/gemini-cli` (identical to macOS once node
# is on PATH from install-brew.ps1). See docs/windows.md.

$ErrorActionPreference = 'Stop'

Write-Host '==> clis (Windows)'
Write-Host '    TODO: implement on Windows reference VM'
exit 0
