{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "features": [
    {
      "id": "feat-mac-one-shot-setup",
      "description": "Single-command bootstrap for a fresh Mac: installs Antigravity, Gemini Desktop, Claude Desktop, Claude Code CLI, antigravity CLI, gemini CLI, supporting Homebrew tooling, and places captured config files.",
      "steps": [
        "scaffold configs/ scripts/ and setup.sh orchestrator",
        "capture current-machine configs into configs/ with secret-stripping",
        "install Homebrew formulae (node, gh, jq, ripgrep, shellcheck, shfmt)",
        "install Claude Code CLI (curl) and Gemini CLI (npm global)",
        "install GUI apps from official DMGs (Antigravity, Gemini, Claude)",
        "link captured configs into OS locations with backup of pre-existing files",
        "orchestrator wires stages with --dry-run / --skip-apps / --only flags",
        "print post-setup auth checklist"
      ],
      "passes": true
    },
    {
      "id": "feat-debian-cli-support",
      "description": "Debian/Ubuntu CLI-only path for setup.sh: OS detection, apt install stage, shared cross-platform CLI/link-configs/verify/auth-checklist scripts, GUI-app stage filtered out.",
      "steps": [
        "OS-detection helper + setup.sh dispatch (macos | debian | else error)",
        "scripts/install-apt.sh — NodeSource + gh apt repos, apt install + shfmt fallback",
        "make scripts/install-clis.sh cross-platform (npm prefix, rc-file pick, Node-version guard)",
        "link-configs.sh skip Mac-only Claude Desktop path on Debian",
        "verify-install.sh skip GUI app + Mac-only-JSON checks on Debian",
        "auth-checklist.sh trim Antigravity + Claude Desktop steps on Debian",
        "docs: README Debian subsection + new docs/debian.md",
        "features.json + progress.md bookkeeping"
      ],
      "passes": false
    },
    {
      "id": "feat-ci-verification",
      "description": "Manually-dispatched GitHub Actions workflow that runs setup.sh / setup.ps1 on fresh Mac, Ubuntu, and Windows runners. Mac uses --skip-apps (GUI installer needs a human); Windows is smoke-only (orchestrator + AST parse). When all three green on one dispatch, feat-debian-cli-support.passes flips true and v1.0.0 ships.",
      "steps": [
        ".github/workflows/ci-tests.yml — workflow with macos-test / ubuntu-test / windows-test jobs, workflow_dispatch only, cancel-in-progress concurrency",
        "README badge + docs update (debian.md future-work pointer at CI)",
        "close out: flip feat-ci-verification.passes and feat-debian-cli-support.passes to true after first three-platform green run; v1.0.0 release"
      ],
      "passes": false
    }
  ]
}
